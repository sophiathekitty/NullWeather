/**
 * loads current weather (all in one) api
 * /plugins/NullWeather/api/current
 */
class SunriseData extends Model {
    constructor(){
        super("sunrise","/plugins/NullWeather/api/sunrise","/plugins/NullWeather/api/sunrise");
    }
}